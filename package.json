{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://ystepane.github.io/mock",
  "devDependencies": {
    "gh-pages": "^6.0.0"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "react-scripts": "^3.0.1"
  }
}
